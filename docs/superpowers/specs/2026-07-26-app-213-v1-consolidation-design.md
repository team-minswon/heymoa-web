# APP-213 v1 재구성 — 표면 인벤토리와 실측

> 캔버스를 한 세대(v1)로 모으기 전에 **앱의 실제 표면을 전수로 재는** 기록이다.
> v1은 "현재 상태"여야 하므로 실측이 먼저고 캔버스 정리가 나중이다. 반대로 하면 또 갈린다.

## 1. 표면 인벤토리

라우트와 오버레이를 코드에서 뽑았다(`app/**/page.tsx`, `error.tsx`, `not-found.tsx`, `loading.tsx`,
그리고 `<Dialog>`·`<Sheet>`·`DropdownMenuContent`를 쓰는 컴포넌트).

| #    | 표면                           | v5 프레임                                                  | 실측                            |
| ---- | ------------------------------ | ---------------------------------------------------------- | ------------------------------- |
| 1    | 랜딩 `/`                       | 없음                                                       | ✅                              |
| 2    | `/terms` · `/privacy`          | 없음                                                       | ✅                              |
| 3    | 인증 모달                      | 없음                                                       | ✅                              |
| 4    | `/auth/callback`               | 없음                                                       | ⚠ 목에서 도달 불가              |
| 5    | `/mock-oauth`                  | 없음                                                       | ✅ (목 전용 — 캔버스 대상 아님) |
| 6    | `not-found.tsx`                | 없음                                                       | ✅                              |
| 7    | `error.tsx`                    | 없음                                                       | ⬜                              |
| 8    | loading 3종                    | 없음 (`j2y3od`는 노트 스켈레톤)                            | ⬜                              |
| 9    | 회의 종료 다이얼로그           | 없음                                                       | ✅                              |
| 9-1  | **새 워크스페이스 다이얼로그** | 없음                                                       | ✅ (인벤토리에 없던 표면)       |
| 9-2  | **새 프로젝트 다이얼로그**     | 없음                                                       | ✅ (인벤토리에 없던 표면)       |
| 10   | 워크스페이스 스위처            | 없음                                                       | ✅                              |
| 10-1 | 노트 행 메뉴 · 유저 메뉴       | 없음                                                       | ⬜                              |
| 11   | 워크스페이스 허브 · 사이드바   | `LHXhy` `cWU64` `sLzX8`                                    | ✅ (APP-209·211)                |
| 12   | 노트 full 4 · side 3           | `Ftvu9` `m0eVmx` `AB8zp` `MFFmb` / `oLmGL` `viNgv` `KCoyt` | 부분                            |
| 13   | 챗봇 5                         | `jsz26` `fQwhV` `lbdN9` `I49sOL` `d6dtU`                   | 부분                            |
| 14   | 알림 3 · 설정 4                | `sPg4o` `e71yPK` `M5pzv` / `WKSCp` `Bt1kk` `V3H0t` `jG86c` | 부분                            |
| 15   | 상태 4                         | `MQYkO` `j2y3od` `ybD9g` `UqTC4`                           | ⬜                              |
| 16   | 예외(권한·잠금·인증 만료)      | 일부                                                       | ⬜                              |

## 2. 실측한 값

### 랜딩 `/`

마케팅 셸(둥근 상단 네비 pill + footer). 히어로(세리프 대제목 + 요약 + `자세히 알아보기`) →
카드 3장(실시간 회의 경청 · 즉각적인 맥락 정리 · 액션 아이템 구조화, 각 325 높이) →
에이전트 라이브러리(Moa Plan/Dev/Design) → 3단계 흐름 → footer. 문서 높이 2847.

### `/terms` · `/privacy`

**둘이 같은 레이아웃이다.** 제목 60px EB Garamond 300, 본문 폭 768 중앙, 섹션 7개,
마케팅 nav + 축약 footer. 문서 높이 2578 / 2606. → **프레임 하나 + 변형 둘**로 충분하다.

### 인증 모달

448×423 · radius 28.8 · 그림자 `0 4px 24px rgba(0,0,0,0.06)` + ring 1.
`Welcome to HeyMoa` (세리프) + 설명 + `Google 로그인` + 약관 고지.

### `not-found.tsx`

마케팅 셸 안. `ERROR 404` 키커 + 세리프 30px `페이지를 찾을 수 없습니다` + 설명 + `홈으로 돌아가기`.

### `/mock-oauth`

`목 환경 전용 화면` + `LINEAR 연결을 허용할까요?` + `허용하고 돌아가기`.

### 다이얼로그 3종 — 전부 같은 치수다

셋 다 `ui/dialog.tsx` 기본값을 그대로 쓴다. 설정 다이얼로그만 호출부에서 값을 명시해
APP-209가 고쳤고, 나머지는 손대지 않아 남아 있었다.

> **2026-07-27 재실측 — 아래가 정본이다.**
> 첫 측정값(radius 22.4 · 그림자 없음)은 **APP-210·APP-222가 없앤 드리프트**다.
> 그대로 그리면 v1에 드리프트를 박는다.

| 무엇            | 크기        | radius | 그림자                |
| --------------- | ----------- | ------ | --------------------- |
| 회의 종료       | **384×221** | **16** | **e3 2연타** + ring 1 |
| 새 워크스페이스 | 384×—       | **16** | 〃                    |
| 새 프로젝트     | 384×219     | **16** | 〃                    |

- padding 16 · gap 16 · 배경 `rgb(245,245,245)` · **border 0** (외곽선은 ring 하나)
- e3 = `0 4 8 #0c0a0914` + `0 20 56 #0c0a092b`

**측정할 때 걸리는 것:** 진입 애니메이션이 `zoom-in-95`라 `getBoundingClientRect`로 재면
384가 아니라 365(= 384 × 0.95)로 잡힌다. 레이아웃 값은 `getComputedStyle`의
`width`·`height`로 읽는다.

### 워크스페이스 스위처

256×163 · radius **16** (2026-07-27 재실측, APP-210 이후) · 배경 불투명(`rgb(245,245,245)`) ·
e3 2연타 + ring 1 · border 0.
항목: 워크스페이스 둘 + `새 워크스페이스` + `워크스페이스 설정`.

### 빈 상태 (노트 없음)

점선 테두리 카드 832×320(`border-dashed`) 안에 원형 아이콘 → 세리프 제목
`아직 회의 기록이 없습니다` → 설명 2줄. 목록 영역 폭을 그대로 쓴다.

### 노트 행 메뉴 · 유저 메뉴

노트 행 메뉴 128×56 · radius 16 · padding 4 · 항목 높이 46 · 항목 radius **8** · 항목 `전체 화면으로 열기` 하나.
유저 메뉴 240×91 · radius 16 · 항목 `내 계정 설정` · `로그아웃`.

둘 다 e3 2연타 + ring 1 · border 0 (2026-07-27 재실측, APP-222 이후).

### 회의 종료 후 (프레임 `UqTC4` 대응)

상단바에서 `중지`·`회의 종료`가 사라지고 **`회의 종료됨` 배지**로 바뀐다.
요약 탭으로 자동 이동하고 `회의를 정리하고 있습니다` + 개요·액션 아이템·인사이트
**스켈레톤 3블록**. **공유 챗봇 트레이가 사라진다.** 레코더 독은 남는다.

## 2-2. 감사가 찾은 결함 — 종료된 회의에서 녹음이 다시 시작된다

**→ APP-214.** 예외 상황을 실제로 밟지 않았으면 안 나왔을 결함이다.

회의 종료 다이얼로그가 "이후에는 이 회의의 전사를 다시 시작할 수 없습니다"라고 약속하는데,
종료 후에도 레코더 독의 `기록 시작`이 활성이고 누르면 **안내 없이** 독이 스피너로 멈춘 채
상단바에 `현재 녹음`이 뜬다. 화면이 `회의 종료됨`과 `현재 녹음`을 동시에 말한다.

세 곳이 어긋나 있다 — UI가 안 막고, 계약에 거절 코드가 없고(`startTranscriptionSession`의 409는
`ACTIVE_TRANSCRIPTION_SESSION` 하나뿐), 목도 `meetingStatus`를 안 본다.

## 2-3. 예외 상황의 도달 가능성

**목으로 밟을 수 있는 것과 없는 것을 갈라 둔다.** 못 밟는 것을 "확인했다"고 적으면 그게 다음
감사를 속인다.

| 예외                        | 도달 | 방법                                               |
| --------------------------- | ---- | -------------------------------------------------- |
| 빈 노트 목록                | ✅   | 새 워크스페이스를 만든다                           |
| 분석 진행 중 스켈레톤       | ✅   | 회의 종료 직후                                     |
| 회의 종료 상태              | ✅   | 회의 종료                                          |
| 알림 빈 상태 · 읽음         | ❌   | 시드에 알림이 항상 있다                            |
| 주 데이터 404 · 분석 FAILED | ❌   | 목 핸들러가 성공만 준다                            |
| 스트림이 종료 없이 끊김     | ❌   | SSE 목이 정상 종료만 한다                          |
| 관전자 · 남의 잠금          | ❌   | `seedForeignLock`이 테스트 전용이고 UI 경로가 없다 |
| 비ADMIN · 만료된 초대       | ❌   | 목 유저가 항상 ADMIN이다                           |
| 인증 만료(세션 게이트)      | ❌   | 목이 항상 인증돼 있다                              |

**도달 못 하는 것은 컴포넌트 코드와 테스트를 근거로 그린다.** 그 사실을 프레임 노트에 적어,
다음 사람이 "실측된 화면"과 "코드로 그린 화면"을 구분할 수 있게 한다.

## 2-4. 드리프트 — 프리미티브 기본값이 스케일 밖이다

**제품 면 오버레이는 e3 2연타 + panel 16이어야 하는데(ELEVATION SPEC), 다이얼로그 셋과
드롭다운 하나가 radius 22.4에 그림자가 없다.**

원인은 표면이 아니라 프리미티브다.

| 프리미티브             | 기본 radius             | 스케일  |
| ---------------------- | ----------------------- | ------- |
| `ui/dialog.tsx`        | `rounded-xl` = **22.4** | 밖      |
| `ui/alert-dialog.tsx`  | `rounded-xl` = **22.4** | 밖      |
| `ui/card.tsx`          | `rounded-xl` = **22.4** | 밖      |
| `ui/dropdown-menu.tsx` | `rounded-lg` = 16       | panel ✓ |

APP-209·211은 **표면별로** 고쳤다(설정 다이얼로그·시트·챗 카드·알림 드롭다운). 그래서 호출부가
값을 명시한 곳만 맞고 기본값을 쓰는 곳은 그대로다.

**이건 APP-210(형태 스케일 일원화)에서 고칠 것이고 여기서 고치지 않는다.** 이 이슈는 실측과
캔버스이고, 프리미티브를 건드리면 90곳이 함께 움직여 감사와 수정이 뒤섞인다.

> **2026-07-27 — 둘 다 끝났다.**
> radius는 APP-210, 그림자는 APP-222다. 프리미티브 기본값이 각각 `rounded-panel`과
> `shadow-e3`가 됐고, 호출부에 중복으로 박혀 있던 `border`도 걷어 외곽선을 ring 하나로 모았다.
> **그래서 위 2장의 첫 실측값은 낡았다** — 재실측값이 정본이다.
>
> APP-210에서 하나 더 나왔다. `cn()`이 커스텀 radius를 tailwind-merge 충돌 그룹으로 보지
> 않아 `rounded-panel rounded-none`이 둘 다 살아남고 CSS 선언 순서가 이겼다. 프레임을
> 그릴 때 "호출부가 명시했으니 그 값이겠지"를 믿으면 안 되는 이유다 — 반드시 실측한다.

## 2-5. 지우려던 옛 세대에 아직 쓸 프레임이 있다

**v5가 못 덮는 표면 중 셋은 v2 행에 이미 그려져 있다.** 캔버스를 "v5만 남기고 지운다"로
처리했으면 이것들을 다시 그려야 했다.

| 프레임                     | 무엇            | 현재 구현과                                                                 |
| -------------------------- | --------------- | --------------------------------------------------------------------------- |
| `A77tqi` (v2/landing)      | 랜딩            | **구조 동일** — 히어로 · 카드 3장 · 에이전트 라이브러리 · 3단계 · footer    |
| `hpQXM` (v2/auth-modal)    | 인증 모달       | **동일** — `Welcome to HeyMoa` · Google 로그인 · 약관 고지                  |
| `y9uef` (v2/auth-callback) | 인증 콜백(로딩) | 구조 같음. 문구만 다름(프레임 `로그인 중입니다` / 구현 `로그인 처리 중...`) |

→ **v1은 v5의 이름을 바꾼 것이 아니라, 세대를 가로질러 현재 상태인 것을 모은 것이다.**

## 2-6. `auth-callback`은 상태가 둘인데 프레임은 하나다

| 상태     | 구현                                                                                   | 프레임   |
| -------- | -------------------------------------------------------------------------------------- | -------- |
| 로딩     | 스피너 + 세리프 `로그인 처리 중...`                                                    | `y9uef`  |
| **실패** | 카드(`rounded-3xl` + 그림자) + 경고 아이콘 + 제목·설명 + `다시 로그인` · `홈으로 이동` | **없음** |

OAuth 실패는 유저가 동의를 취소하면 나는 **정상 경로**인데 그려진 적이 없다.

## 3. 판정 — 규칙이 갈리는 곳

**마케팅·인증 면은 제품 면 규칙 대상이 아니다.** FORM SPEC의 대문자 키커 금지와 ELEVATION SPEC의
e2/e3 2연타는 제품 화면에 한정된다(SPEC 본문이 "제품 면", "마케팅(랜딩·약관) 전용"이라고 갈라 둔다).

따라서 아래는 **드리프트가 아니다.**

| 무엇                                     | 값           | 이유      |
| ---------------------------------------- | ------------ | --------- |
| 인증 모달 radius 28.8 · 단일 티어 그림자 | 스케일 밖    | 인증 면   |
| 404의 `ERROR 404` 키커                   | 대문자       | 마케팅 셸 |
| 랜딩 카드의 단일 티어 그림자             | `0 4px 16px` | 마케팅 면 |

**`/mock-oauth`는 캔버스에 넣지 않는다.** 목 전용 화면이라 제품이 아니다.

## 4. 방법 — fullPage 캡처를 믿지 않는다

랜딩을 `fullPage`로 찍었더니 카드 3장 섹션이 **통째로 비어 나왔다.** DOM은 opacity 1이었고
뷰포트 단위로 다시 찍으니 정상이었다. 스티칭 문제다.

그대로 믿었으면 "랜딩에 340px 빈 띠" 라는 **없는 결함**을 이슈로 만들 뻔했다.
→ 이 감사는 **뷰포트 단위 캡처 + `getComputedStyle` 실측**을 함께 쓴다.

## 4-1. side 프레임 셋이 회의 조작의 자리를 말한다 (2026-07-27, APP-220)

`oLmGL`·`viNgv`·`KCoyt` 셋을 열어 대조했다.

| | side 3장 | full (`Ftvu9`) |
| -- | -- | -- |
| 상단 회의 조작 | **없음** | `● 녹음 중` · `중지` · `회의 종료` |
| 레코더 독 | **없음** | 하단 중앙 (마이크 + 12:34 + 파형) |
| 주 액션 | **`확장`** + 닫기 | — |

**side는 읽기·미리보기 면이다.** 코드가 side에도 독을 그려서 "녹음은 시작되는데 회의를
끝낼 곳이 없는" 상태였고, APP-220이 프레임대로 맞췄다.

**다만 셋 다 종료된 회의다.** 라이브 회의를 side로 볼 때는 그려진 적이 없다 — ②단계에서
그 상태를 그릴 때 "도는 녹음의 독을 남긴다"는 현재 구현(추론)이 맞는지 함께 정한다.

## 5. 남은 것

- `error.tsx` — 런타임 에러를 일부러 내야 해서 목으로는 안 잡힌다
- loading 3종 — 순간적이라 네트워크를 늦춰야 잡힌다
- 프레임이 있는 제품 면 26장의 재대조 (APP-209·211이 일부만 봤다)
- 2-3 표에서 ❌인 예외들 — 코드 기준으로 그리고 그 사실을 프레임 노트에 적는다

## 5-1. ②단계 진행 — 그린 프레임

| 프레임                    | 무엇     | 상태                                      |
| ------------------------- | -------- | ----------------------------------------- |
| `m6aV4` `v5/static-terms` | 이용약관 | ✅ nav 76 + 본문 1303 + footer 343 = 1722 |

**본문 문구는 앱에서 가져왔다.** 약관 텍스트를 창작하면 그게 정본이 되므로, 실제 화면의
리드 문장과 7개 섹션 본문을 읽어 그대로 넣었다.

**nav와 footer는 `A77tqi`(v2/landing)에서 복사했다.** 마케팅 셸은 이미 그려져 있고
현재 구현과 구조가 같다(2-5 참조) — 다시 그릴 이유가 없다.

### 캔버스 작업에서 걸린 것

- `batch_design`의 인자 이름은 `input`이다(`snippet` 아님)
- 프레임 높이를 고정값으로 두면 내용보다 커서 빈 공간이 남는다 → `fit_content`
- `snapshot_layout`은 `nodeId`를 줘도 **캔버스 전체**를 돌려준다. 프레임 확인은
  `get_screenshot`으로 한다

## 6. 다음 단계로 넘어가기 전에

①단계에서 나온 것을 정리하면 이렇다.

- **표면 인벤토리는 코드만으로 완성되지 않는다.** 새 워크스페이스·새 프로젝트 다이얼로그는
  실제로 눌러야 나왔다
- **프레임이 없는 표면이 8개** (랜딩·약관 2·인증 모달·404·다이얼로그 3·드롭다운 3)
- **드리프트는 프리미티브에 있다** (APP-210)
- **결함 하나** (APP-214)
- **예외의 절반은 목으로 못 밟는다** (2-3)

②단계(프레임 그리기)는 이 실측값을 입력으로 쓴다. ③단계(캔버스 정리)는 그 뒤다 —
지금 지우면 대조할 근거가 사라진다.
