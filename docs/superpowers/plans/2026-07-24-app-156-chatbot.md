# APP-156 챗봇 2종 + MSW 풀 계획

## Task 1: ChatComposer 프리미티브

**Files:** `components/chat/chat-composer.tsx`(신규), 테스트.

- form + Input + 전송/중지 버튼(원형 ↑ ArrowUp / Square). props: `draft·onDraftChange·onSubmit·onStop·isBusy·isStreaming·placeholder·footer`.
- 단위 테스트: 스트리밍이면 중지, 아니면 보내기; submit 콜백; footer 렌더.

## Task 2: 두 챗봇 배선

**Files:** `shared-chat-panel.tsx`, `personal-chat.tsx`.

- `shared-chat-panel` active 케이스 → `ChatComposer`(placeholder "이 회의에 대해 물어보세요"). phase 게이트/ComposerNotice 유지.
- `personal-chat` 인라인 form → `ChatComposer`(placeholder 세션 유무, footer=승인 대기 안내).

## Task 3: MSW 응답 풀

**Files:** `lib/mocks/chat-stream.ts`, `chat-stream.test.ts`.

- 일반 응답 한 줄 고정 → 2~4문장 풀에서 `faker.seed(hash(chatId:turn:message))` 후 `arrayElement`. `message_end.content`=토큰 이어붙임 유지.
- 테스트: 같은 입력 결정적, 응답이 풀 소속, 승인/에러 경로 불변.

## Task 4: 게이트·검증·머지

- 컴포넌트/스트림 테스트, `pnpm test:run && lint && typecheck && build && test:e2e`.
- Playwright 공유(노트 트레이)·개인(플로팅) 실측 — 같은 입력·행, 밀도.
- `codex exec review --base dev` P1 없음 → dev squash merge.

## Self-Review
- 계약·SSE 경로 불변. 승인 카드/게이트 동작 유지.
- 순수 함수 결정성: 시드가 입력 파생 — 테스트 안 흔들림.
